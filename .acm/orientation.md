# orientation.md — pea-skills-lite

_Last updated: 2026-07-03 (run: cold-read-verification-and-defect-repair)_

## Current claims

1. **The mechanism layer remains zero-corrections; the explanation layer is a confirmed recurring finding-class.** Across the entire trail — two external runs, a Kaizen audit + CHECK phase, a comparative fidelity audit, and now a cold-read test — no audit has ever found a broken mechanism. Every finding has been the same class: something used but not explained (lens glosses → marker glosses → the cold-read's 7 defects: dangling `§4` reference, unexplained `.acm-root`, phantom "four across-trail triggers" never enumerated, no Tier 2 example, unglossed "finding-class", unspecified `destination.md` shape, fuzzy read-window). Three separate audit styles, one defect class.

2. **Author self-verification cannot test the stranger-pickup bar — now demonstrated, not just suspected.** The v2.0.0 rewrite's author verified mechanism-preservation correctly (checklist held) but predicted the cold read would pass; a zero-context subagent returned FAIL with 7 defects the author read past. Falsifiable operational rule derived: any change claiming to serve the stranger-pickup bar needs a zero-context reader before the claim is made, not after.

3. **v2.x (full authorial rewrite by Claude Fable 5, then cold-read repair) preserved all mechanisms and encoded the operator's measurement philosophy.** Scores are temporary plateau-diagnostics, void on destination-shift, never standing targets — now in step 2 of the skill itself, converted from conversation insight to mandate by explicit operator instruction.

4. **Standing prediction from the rewrite entry:** accretion seams (locally-correct patches, globally uneven texture) will reappear by roughly the 5th-6th post-rewrite patch, warranting another coherence pass. The 2.0.1 defect repair is patch #1 against that clock.

## What the next runs should test

- **Real code repo, structural stakes** — still the only untested surface, and the only way the depth-tier judgment gets tested under pressure. Deferred across the whole arc; the deferral itself is now a named pattern (see prior orientation's rule about restating without acting).
- A second zero-context cold read after the 2.0.1 repairs, to confirm the defect list actually closed (this run repaired but did not re-test — one open loop, disclosed).

## Active operational rules

- Before claiming the stranger-pickup bar is met, run a zero-context reader against the file. Author verification only covers mechanism-preservation.
- Before encoding an unconfirmed inference into a committed file, ask first if answerable in one turn (carried from previous orientation — still the operative bootstrap discipline).
- Scores shown to the operator are framed as plateau-relative diagnostics, disposable on destination-shift.

