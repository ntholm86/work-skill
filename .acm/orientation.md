# orientation.md - work-skill

## Work current orientation

**Last updated:** 2026-07-27 (`define-problem-and-optional-harness-memory`, v3.12.0 bounded candidate silence)

**Destination basis:** `Destination refinement - 2026-07-27 (the problem Work solves and optional harness memory)`

### Current rubric

| # | Destination-derived measurement | /10 | Current evidence |
| --- | --- | ---: | --- |
| 1 | Intent fidelity over literal compliance | 9 | The problem statement and Understand step now require purpose, provenance/authenticity, and consequence checks; behavior beyond authored instruction remains untested. |
| 2 | Practical context continuity | 9 | Destination, current-first Orientation, selective Trail lookup, hunching, and the public contract now accurately name workflow-level prevention/detection/recovery without claiming hidden-cognition control. |
| 3 | Temporal and provenance-separated memory | 9 | Operator Destination, replaceable Orientation, agent Trail, and proxy-authored session evidence have explicit roles and disagreement handling. |
| 4 | Optional harness integration | 8 | SKILL and README know the canonical repo, intended path, assurance modes, selective lookup, and migration status; no real `.acm/sessions/` run has exercised them. |
| 5 | Assurance honesty | 9 | Work now states provider-reasoning, streaming, session-verification, authorization, causality, and current-migration ceilings. |
| 6 | Selective evidence cost | 9 | Session reads are trigger-bound and narrow by identifiers, timestamps, metadata, or content search; behavior is not yet independently verified. |
| 7 | Operator consent and privacy | 9 | Install/update/start/routing require consent, checks are triggered rather than routine, sessions are read-only, and public docs describe them as private evidence. |
| 8 | Route neutrality and standalone portability | 9 | Work remains a one-file, target-agnostic skill; the companion can remain optional without weakening that identity. |
| 9 | Improve-grade decision reasoning | 9 | The run separated stable problem from mechanism, rejected mandatory/always-read routes, disclosed a patch-placement reversal, and bounded claims to verified evidence. |
| 10 | Observable Autonomy | 8 | Semantic decisions are durable and Work can consume independent evidence, but correlation metadata is future proxy work and this run is explicitly standalone. |

**Plateau: 8.8/10.** This disposable diagnostic credits the v3.12.0 instruction and public-contract conversion, not unexercised harness behavior; it is neither comparable to the discarded prior-Destination plateau nor a standing target.

### Rubric changes

The old map is void. Authorship and hunch completion remain active constraints but no longer warrant top-level measurements. The new Destination makes intent fidelity, practical context continuity, provenance-separated memory, optional harness evidence, privacy/consent, and assurance honesty independently measurable. Route neutrality, decision quality, cost, and Observable Autonomy remain because they constrain any acceptable implementation.

### Current claims

1. **Work explicitly provides workflow-level context continuity.** It externalizes operator intent, reloads current state, and selectively recovers historical learning; it does not claim perfect hidden cognition.
2. **`.acm/sessions/` is now a fourth optional evidence source.** It strengthens provenance and temporal review without becoming hot-path context or a hand-authored state file.
3. **Standalone and harness-backed Work are distinct assurance modes.** A directory's existence alone does not prove current-run capture; relevant session evidence must be verified, and this v3.12.0 run remains standalone.

### Highest-leverage todo

Bounded candidate silence on the Work-side contract. Reopen when a migrated proxy provides real `.acm/sessions/` evidence, a compatibility/version flow can be exercised, an independent executor misuses the assurance boundary, or another concrete failure appears. The proxy storage migration remains a separate target.

### Active operational rules

- Judge success against the operator's actual purpose, provenance needs, and consequences; literal prompt completion is insufficient when it defeats that purpose.
- Describe context preservation at the workflow level: externalize, reload, detect drift, and recover. Never imply perfect access to or control of hidden model cognition.
- Treat `.acm/sessions/` as optional, independently authored evidence memory. Query relevant sessions only on risk, contradiction, provenance, audit, or challenged self-report; never load all traffic by default.
- Preserve provenance boundaries: the operator authors Destination, Work authors semantic Trail, Orient replaces current state, and the proxy alone authors session JSONL. Preserve disagreements rather than silently reconciling them.
- Do not claim harness-backed assurance without verifying relevant session evidence for this run. Reasoning payloads may be absent, provider-specific, incomplete, or not causally faithful; streaming may release output before final persistence; the harness does not supply intent or authorization.
- Never silently install, update, start, or route traffic through the companion. Use `https://github.com/ntholm86/llm-harness-proxy`; check versions only on setup, explicit assurance need, incompatibility, or stale/unknown state, and ask for operator consent before changing the environment.
- Keep Destination authority with the operator; agent hunches remain sourced questions until confirmed.
- Credit Nils W. Holmager as Work's canonical author on current public and distributable surfaces; model identities are execution provenance.
- Distinguish universal reasoning operations from target-specific solution principles and constraints.
- Read this top Work current-orientation block before Standard or Full reasoning; consult Trail history only on an explicit escalation trigger.
- For ordinary historical escalation, search relevant realization/reversal markers and read only needed context; reserve the full substantive marker inventory for an actual Orient.
- Keep Improve-grade decision operations constant while domain risk scales evidence and Trail tier scales durable narration.
- Preserve the enclosing mission across instrumental targets through both repository-local Trail records.

### Tested outside-rubric concern

Putting sessions under `.acm/` could encourage broad context loading, accidental publication, or agent mutation of independent evidence. The acceptable design keeps sessions private by default, selectively read, proxy-authored, and optional.

### Loop-effectiveness notes

The operator's correction legitimately reopened bounded silence and changed the problem model. The open pass disconfirmed a documentation-only route because the live skill's three-file memory contract could not discover the desired evidence. v3.12.0 converted the problem statement, optional evidence path, assurance modes, selective retrieval, limits, and consent boundary. Validation passed, but no harness-backed behavior was available to test. A duplicated Destination gate 5 surfaced and remains deferred rather than mixed into this change.

Watch for: companion language turning optional evidence into a dependency, directory presence being mistaken for current-run capture, provider reasoning being overstated, raw sessions entering ordinary context, or the proxy migration being falsely described as already implemented.
